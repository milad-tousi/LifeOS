export const AUTH_SESSION_STORAGE_KEY = "lifeos.auth.session";

export const DEFAULT_LOCAL_AUTH_USER = {
  email: "admin@lifeos.local",
  password: "admin123",
  displayName: "Admin",
  role: "admin",
} as const;
