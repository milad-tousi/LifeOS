export function StepWelcome(): JSX.Element {
  return (
    <div className="onboarding-copy">
      <p>
        LifeOS uses a small set of personal preferences to shape routines, reminders, and future
        guidance around your real life.
      </p>
      <p>
        Everything stays on this device. There is no cloud sync, no backend, and no identity
        verification in this setup flow.
      </p>
    </div>
  );
}
