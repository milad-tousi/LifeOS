# Capacitor Preparation

This folder reserves the integration boundary for adding Capacitor later.

When mobile packaging is introduced:

1. Add Capacitor dependencies.
2. Generate native projects.
3. Keep native plugin configuration isolated from the web app source tree.
4. Route device capabilities through the existing service layer.

